# DroneDoctor 项目现状审计

**审计日期**：2026-06-21

**目标版本**：v1.3.0 需求验证版

**结论**：代码已切换为“微信引流 + 免费兑换券 + 匿名限时体验”路径，普通账号和付费会员不再出现在公开产品流程中。

## 已完成

- 公开注册接口返回 `410 PUBLIC_ACCOUNTS_RETIRED`；
- 普通历史账号不能登录，登录入口仅供管理员使用；
- 个人中心、历史记录和“我的反馈”页面已下线；
- 历史记录 API 已停止挂载；
- 兑换券无需绑定账号，可激活 3 天匿名体验通行证；
- 重复兑换采用数据库条件更新，避免同一券码并发重复使用；
- 每次使用通行证都会核验券状态和到期时间，管理员可撤销已激活通行证；
- 管理后台可区分生成、实际发放、激活、诊断开始、诊断完成和反馈；
- 反馈允许匿名提交，并可留下微信或手机号；
- PostgreSQL 迁移纳入生产启动流程，禁止危险的基线回滚；
- 健康检查会验证数据库连接并返回版本号；
- 请求日志包含请求标识，部署包、数据库备份、校验和与恢复流程已有文档；
- 当前运行数据文件不再纳入 Git 跟踪。

## 当前约束

- 管理员账号仍保留，这是生成兑换券、查看反馈和市场数据所必需的内部能力；
- 数据库仍保留历史用户表和部分兼容字段，本版本通过路由与鉴权下线公开账号能力，没有执行破坏性删表；
- 仓库历史提交曾包含运行数据。当前工作树已停止跟踪，但远端 Git 历史清理和相关凭据轮换仍需单独授权执行；
- 本次没有执行线上部署，也没有变更生产数据库；
- Docker Compose 配置已验证，但本机 Docker Desktop 未运行，因此未完成实际镜像构建。

## 验证结果

| 检查项 | 结果 |
|---|---|
| 后端 JavaScript 语法检查 | 58 个文件通过 |
| 后端自动化测试 | 4 个套件、43 项测试通过 |
| 前端通行证测试 | 3 项测试通过 |
| 前端生产构建 | 通过 |
| Docker Compose 配置解析 | 通过 |
| Git 差异格式检查 | 通过 |
| 本地匿名兑换券激活与重复兑换 | 通过 |
| 本地管理员、通行证和管理接口权限隔离 | 通过 |
| 本地健康检查和市场指标 | 通过 |
| Docker 镜像实际构建 | 未执行，本机 Docker 服务未启动 |

## 发布前必做

1. 先备份生产数据库并验证备份可读取。
2. 设置 `APP_VERSION=v1.3.0`，使用版本包和 SHA-256 校验发布。
3. 执行数据库迁移后检查 `pgmigrations` 包含：
   - `001_initial_schema`
   - `002_trial_access_and_feedback`
4. 验证健康检查、管理员登录、发券、激活、诊断和反馈完整链路。
5. 发布后观察错误日志和兑换券指标，发现异常时先止血，再判断代码回滚或向前修复。

## 单独风险事项

Git 历史数据清理会重写提交并影响远端协作，不能与普通发布混在一起执行。建议在确认备份、轮换可能泄露的凭据、通知协作者后，单独进行历史清理和强制推送。
